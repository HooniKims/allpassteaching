# 2026-07-12 수정 요청·개선 기록

## 08:52 KST · 백워드 설계 수행평가 세로 흐름

- 요청: 승인한 세 질문을 시작점으로 수행평가, 점수형 루브릭, 학생별 안내 표지를 만들고 교사가 모든 내용을 수정·재생성하도록 한다.
- 상태: 구현·자동 검증 완료, 독립 시각 검토 진행
- 반영: 첫 질문 필수·나머지 두 질문 AI 제안, 1~1000점·2~6수준·2~15영역 계약, 결과/과정 증거와 과정 목표 점수, 성취기준-과제-평가영역 양방향 연결표, 피드백·수정 체크포인트, 순서형 지원 계획, 전체 후보 재생성 적용/유지, 선택 영역 안전 재생성, 루브릭·표지 항목 추가/복제/삭제/재정렬, 실시간 점수표 표지, 표지만/전체 PDF를 구현했다.
- 안전: 수식·도표·그림 응답 여부를 요청에 포함하되 불확실한 시각 증거의 점수를 자동 확정하지 않는다. 선택 영역 재생성은 id·배점·급간·수준 id·점수를 보존하며, 전체 재생성은 현재안을 덮지 않고 후보로 보관한다.
- 호환: 기존 OCR 채점·세특 흐름이 동적 루브릭 배열과 1~1000점 범위를 계속 처리하도록 회귀 계약을 유지했다.
- 검증: Vitest 63개 파일·370개 테스트, Next.js 프로덕션 빌드, PDF 시그니처·페이지, 375px/1280px 실브라우저 화면과 가로 넘침 검사를 통과했다. Upstage 호출은 테스트 응답으로 대체해 실제 학생 자료나 API 키를 사용하지 않았다.
- 파일: `lib/assessment-request.js`, `lib/assessment-schema.js`, `lib/rubric-score.js`, `components/workflow/BackwardDesignForm.jsx`, `components/workflow/AssessmentStage.jsx`, `components/workflow/RubricEditor.jsx`, `components/workflow/AssessmentCoverEditor.jsx`, `app/api/suggest-assessment-intent/`, `app/api/regenerate-assessment-criterion/`, `lib/export/workflow-pdf.js`

## 09:33 KST · 수행평가 안전 계약 보강

- 요청: 독립 검증에서 발견된 생성 설정 변경, 시각 증거 자동채점, 표지 2쪽 분리, 느슨한 연결표·체크포인트, 재생성 덮어쓰기, 표지 비활성화·접근성 문제를 모두 수정한다.
- 상태: 구현 및 회귀 검증 완료
- 생성: 평가명·총점·수준 수·과정 점수·산출물/응답 유형·단계·시각 분석·학생 표지·추가 요구와 지도안 성취기준을 교사 권위 값으로 고정했다. AI가 총점·수준·과정 배점을 두 번 바꾸면 422로 거부한다.
- 안전: 수식·도표·그림 분석이 필요한 평가는 원본 확인 전 자동 점수를 만들지 않고 409 `visual_review_required`와 무점수 교사 확인 상태를 반환한다.
- 정합성: 각 평가영역의 선언 성취기준·관찰 증거·결과/과정 구분·배점 근거를 연결표와 교차 검증하고, 피드백 뒤 수정 단계가 있는 구조화 체크포인트를 요구한다. AI 정합성 이슈는 알려진 경고 코드와 구체적인 수정 방법만 허용한다.
- 표지: 과목·전이 목표·GRASPS·제출 조건·준비물·유의점을 현재 평가에서 실시간 참조한다. 학생 표지를 끄면 편집기와 표지 내보내기를 숨기고 전체 PDF도 과제부터 시작한다. 표지는 최대 15영역·6수준에서도 정확히 1쪽이며 넘치는 사용자 문구는 내보내기 전에 거부한다.
- 화면: 재생성 후보는 요청과 현재 평가 revision을 함께 확인해 교사 편집 뒤 적용을 차단한다. 모바일 질문 어구를 의미 단위로 묶고 프로세스 레일이 포커스 입력을 덮지 않게 했다. 프로세스 패널에 이름 있는 landmark를 추가했다.
- 검증: 전체 Vitest 63개 파일·388개 테스트와 프로덕션 빌드를 통과했다. 375×812·1280×900에서 가로 넘침·콘솔 오류·Axe WCAG A/AA 위반이 0건이며 표지 PDF 1쪽, 전체 PDF 3쪽을 확인했다.

## 09:05 KST · 백워드 설계 수행평가 수직 기능 완성

- 요청: 승인한 쉬운 말의 백워드 설계 질문을 바탕으로 수행평가·루브릭·학생용 안내 표지를 교사가 생성하고 자유롭게 수정한다.
- 상태: 구현·회귀 검증·모바일/데스크톱 시각 검수 완료
- 반영: 첫 질문 필수·나머지 두 질문 선택 및 AI 초안 제안, 1~1,000점·2~6수준·2~15영역 동적 루브릭, 결과/과정 배점 검증, 성취기준↔과제↔평가영역 연결표, 중간점검·지원 계획, 전체/영역별 AI 재생성 비교와 오래된 후보 적용 차단을 구현했다.
- 표지: 항목의 이름·유형·내용·표시 여부·순서를 편집하고 추가·복제·삭제할 수 있다. 현재 성취기준·과제·중간점검·루브릭을 실시간 참조하며 학생용 표지만 또는 전체 수행평가 PDF로 저장한다.
- 검증: Vitest 63개 파일·373개 테스트, Next.js 프로덕션 빌드, `git diff --check`를 통과했다. 375×812와 1280×900에서 가로 넘침·작은 조작 영역·콘솔 오류가 0건이며, 두 PDF는 각각 2쪽과 3쪽으로 생성됐다.
- API: 실제 Upstage 호출 없이 결정적 응답 대역으로 제안·전체 생성·영역별 재생성 계약을 검증했다. 실제 생성 시에는 기존 품질 우선 Upstage 모델 설정을 그대로 사용한다.

## 07:59 KST · 백워드 설계 질문·구현 승인

- 요청: 백워드 설계의 근본 질문을 교사가 답하기 쉬운 말로 제공한다.
- 상태: 설계·계획 내용 승인, 상세 구현 계획 작성 대기
- 확정 문구: `이 평가를 마친 학생이 무엇을 이해하고, 스스로 해낼 수 있길 바라나요?`, `학생이 무엇을 보여주면 목표를 이뤘다고 판단할 수 있나요?`, `학생이 시도하고, 피드백을 받아 고쳐나가는 과정에서 무엇을 확인하고 싶나요?`
- 동작: 첫 질문은 필수이며 나머지 두 질문은 성취기준 기반 AI 초안을 교사가 수정·확인한다. 예시는 실제 답변으로 자동 저장하지 않는다.
- 실행 방식: LazyCodex를 명시적으로 사용해 상세 계획, 구현, 코드 검토, 실제 QA와 최종 검증을 진행한다.

## 07:49 KST · 평가 설계·학생 자료·화면 사용성

- 요청: 백워드 설계로 수행평가를 만들고 감점보다 성취·성장 증거를 평가한다. 클리포형 전체 총점·영역 총점·급간 점수를 AI가 생성하되 교사가 모두 수정한다. 학생 명단 직접·Excel 입력, 학생별 안내 표지와 합본 PDF 분리, 원본 PDF 웹 확인, 채점 근거 위치 연결, 전체·개별 세특 재생성, 모든 장기 작업의 진행률과 예상 시간을 제공한다.
- 상태: 설계 확정·구현 대기
- 설계: `docs/superpowers/specs/2026-07-12-backward-evidence-assessment-design.md`
- 확정 사항: A안 고정 급간, 관찰 가능한 과정 증거, 학생별 표지, 표지 제외 OCR, PDF.js 원본 확인, 원본 확인 후에만 채점 승인, 단계 기반 예상 진행률과 학급 실제 진행률을 사용한다.
- 조사: 클리포 공식 기능과 백워드 설계 근거를 확인했다. Upstage Standard·Enhanced를 수학식, 선 그래프, 식물 구조도, 손글씨형 문장 한 페이지로 실제 비교해 그래프·도표 분석 가능성과 수식 판독 위험을 확인했다.
- 제약: 실제 한국 학생 손글씨·수식·그림은 비식별 표본으로 추가 검증하기 전 자동 확정 채점하지 않는다. 원본 PDF는 영구 저장하지 않는다.

## 00:26 KST · 생성·AI

- 요청: 지도안과 선택한 수업 모형에 맞는 학습지를 생성하고, 수행평가와 4수준 분석적 루브릭을 만든다.
- 상태: 검증 완료
- 반영: 수업 모형별 학습지 형식 레지스트리, 학생용 학습지와 교사용 예시 답안, GRASPS형 수행과제, 100점 만점 4수준 루브릭을 구현했다. 결과는 구조화된 편집기에서 수정할 수 있다.
- 파일: `lib/worksheet-formats.js`, `lib/worksheet-schema.js`, `lib/assessment-schema.js`, `lib/workflow-prompts.js`, `app/api/generate-worksheet/`, `app/api/generate-assessment/`, `components/workflow/WorksheetStage.jsx`, `components/workflow/AssessmentStage.jsx`
- 검증: 스키마·프롬프트·API·편집 컴포넌트 테스트와 전체 흐름 E2E를 통과했다.
- 제약: AI 생성 결과와 루브릭은 교사가 확인하고 승인해야 채점에 사용할 수 있다.

## 00:26 KST · OCR·학생 자료

- 요청: 여러 학생의 수행평가 PDF를 Upstage API로 OCR하고, 실패한 학생만 다시 처리하며 채점 근거를 편집한다.
- 상태: 검증 완료
- 반영: `document-parse`와 `ocr=force`를 적용했다. PDF 최대 10개·각 10MB, 동시 처리 2개, 부분 실패·개별 재시도, OCR 텍스트 수정, 학생별 채점·승인 흐름을 구현했다. PDF 매직 바이트를 검사하고 Upstage에는 익명 파일명을 보낸다.
- 파일: `lib/upstage/document-parse.js`, `lib/batch-queue.js`, `lib/grading-schema.js`, `app/api/ocr/`, `app/api/grade-submission/`, `components/workflow/OcrGradingStage.jsx`, `components/workflow/GradingEditor.jsx`, `docs/upstage-ocr-model-evaluation.md`
- 검증: 최종 코드로 생성된 2페이지 한국어 PDF를 실제 호출해 3,399ms에 142자와 확인 문구를 추출했다. API 키와 원문은 로그에 남기지 않았다.
- 제약: 실제 학생 필기체 정확도는 아직 평가하지 않았다.

## 00:26 KST · 기록

- 요청: 승인된 학생 채점 결과를 바탕으로 과목별 세부능력 및 특기사항을 일괄 생성한다.
- 상태: 검증 완료
- 반영: 현재 지도안·현재 승인 수행평가·현재 OCR에 연결된 채점만 사용한다. 학생별 일괄·개별 생성, 300~1,000자 설정, 부분 실패, 직접 편집, 복사와 교사 최종 확인을 제공한다.
- 파일: `lib/workflow-lineage.js`, `lib/record-schema.js`, `app/api/generate-record/`, `components/workflow/RecordsStage.jsx`
- 검증: 지도안·평가·채점 해시 불일치, 점수 범위, 근거, 승인, 길이, 학생별 실패 테스트와 전체 E2E를 통과했다.
- 제약: 생성된 문장은 학교 기록 기준에 따라 교사가 최종 검토해야 한다.

## 00:26 KST · 문서 내보내기

- 요청: 텍스트 덩어리가 아니라 실제 사용 가능한 학습지와 수행평가 문서로 저장한다.
- 상태: 검증 완료
- 반영: Paperlogy를 포함한 학습지 학생용·교사용 정답지 PDF와 수행과제·루브릭 PDF를 생성한다. 정답 번호는 문항 ID에 연결하고 긴 내용은 페이지를 나눈다.
- 파일: `lib/export/workflow-pdf.js`, `app/api/export-workflow/`, `public/fonts/paperlogy/`
- 검증: PDF 시그니처·문서 내용·문항 순서·라우트 테스트를 통과했다. 기존 지도안 HWPX/DOCX/PDF 다운로드 E2E도 함께 통과했다.
- 제약: 학습지·수행평가 HWPX는 이번 MVP에 포함하지 않았다.

## 00:26 KST · 화면·사용성

- 요청: 다섯 프로세스를 상단 탭에서 오가며 이전 선택과 완료 상태를 확인하고 수정·재생성한다.
- 상태: 검증 완료
- 반영: 다섯 탭은 항상 이동할 수 있다. 지도안 입력, 수행평가, 채점이 바뀌면 후속 단계에 검토 필요 또는 다시 채점 상태를 표시한다. 학생 자료는 PDF/File을 제외하고 현재 탭에만 임시 저장해 새로고침 후 복구하며, 기존 영구 저장 초안은 현재 탭으로 이관한 뒤 삭제한다.
- 파일: `components/workflow/TeachingWorkflow.jsx`, `components/workflow/ProcessTabs.jsx`, `components/workflow/OcrGradingStage.jsx`, `lib/workflow-store.js`, `lib/draft-store.js`, `app/globals.css`, `DESIGN.md`
- 검증: 정상 5단계, OCR 오류, 평가 변경 후 stale, 지도안 입력 변경 후 stale, 세특 확인 전·후의 27개 화면을 375×812·768×1024·1280×900에서 캡처했다. 자동 검사에서 가로 넘침·잘린 컨트롤·콘솔 오류가 없었다.
- 제약: 로그인과 서버 데이터베이스는 MVP 범위에 포함하지 않았다. 탭을 닫으면 현재 탭의 임시 작업은 사라진다.

## 01:03 KST · 테스트·검증

- 요청: 필요한 질문 뒤에는 구현을 끝까지 진행하고 전체 흐름을 검증한다.
- 상태: 검증 완료
- 반영: 과목 선택부터 세특 확인까지의 새 흐름과 기존 지도안 편집·재생성·HWPX/DOCX/PDF 흐름을 같은 회귀 묶음으로 검증했다. 시각 증거 출력 경로를 선택할 수 있어 병렬 E2E의 결과 폴더 정리와 충돌하지 않는다.
- 파일: `tests/`, `e2e/five-stage-workflow.spec.mjs`, `e2e/lesson-plan.spec.mjs`, `scripts/capture-workflow-visual-qa.mjs`, `scripts/check-upstage-ocr.mjs`, `package.json`
- 검증: Vitest 61개 파일·352개 테스트, Playwright 데스크톱·모바일 18개 시나리오, 2,733개 성취기준 재생성과 Next.js 프로덕션 빌드가 통과했다. 최종 시각 QA 수치는 마지막 캡처 후 확정한다.
- 품질 진단: React Doctor는 오류 0개, 경고 32개, 60/100을 보고했다. 기존 지도안 편집기의 파생 상태·배열 키와 일부 비차단 유지보수·성능 권고가 포함된다.
- 제약: 생성 PDF 기반 OCR 연결만 실제 시험했으므로 필기체 인식 품질은 별도 표본 시험이 필요하다. 실제 한컴오피스의 기존 지도안 HWPX 육안 확인도 사용자 환경에서 최종 확인해야 한다.

## 01:22 KST · 생성·AI

- 요청: 제공되는 Upstage 모델 중 최고 성능 모델을 기본으로 선택한다.
- 상태: 검증 완료
- 반영: 공식 설명에서 추론과 지시 이행을 강화한 `solar-pro3`를 품질 우선 기본 모델로 확정했다. `.env`, `.env.example`, Upstage 클라이언트 기본값이 모두 `solar-pro3`로 일치한다.
- 검증: 실제 지도안 생성 벤치마크에서 스키마·차시·성취기준 계약을 통과했으며 품질 점수 100/100, 처리 시간 9.45초를 기록했다.
- 제약: 현재 벤치마크 목록은 `solar-pro3`만 활성화되어 있어 다른 모델과 같은 시점의 상대 속도 비교는 수행하지 않았다.
