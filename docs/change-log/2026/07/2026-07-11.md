# 2026-07-11 수정 요청·개선 기록

## 21:39 KST · 화면·사용성

### 요청 1 · 수업 개요 가운데 정렬

- 요청: 표준 수업 지도안의 `일시`부터 `수업모형`까지 짧은 개요 값을 가운데 정렬한다.
- 상태: 검증 완료
- 반영: 웹 편집 표와 HWPX의 날짜·장소·대상 학급·수업자·학교급·과목·단원·수업 제목·차시·수업 모형 값을 가운데 정렬했다. 성취기준과 긴 본문은 읽기 편하게 왼쪽 정렬을 유지한다.
- 파일: `components/lesson-plan/OverviewTable.jsx`, `app/globals.css`, `lib/export/hwpx.js`
- 검증: 컴포넌트·문서 모델·HWPX 정렬 회귀 테스트 추가 및 통과.
- 제약: 한컴오피스 앱에서의 최종 육안 검사는 남아 있다.

### 요청 2 · 실제 수업 날짜와 교시 입력

- 요청: `일시`에서 `T`와 현재 시각을 제거하고, 수업 정보 단계에서 교시를 직접 입력한다.
- 상태: 검증 완료
- 반영: 날짜 입력과 1~12 교시 입력을 분리했다. 현재 시각을 자동 삽입하지 않으며 기존 `datetime-local` 값은 날짜만 안전하게 마이그레이션한다.
- 파일: `components/lesson-plan/LessonBasicsStep.jsx`, `lib/lesson-input.js`, `lib/draft-store.js`, `lib/export/document-model.js`
- 검증: 날짜 정규화, 교시 표시, 초안 마이그레이션 테스트 통과.
- 제약: 교시 범위는 일반 초·중·고 사용을 기준으로 1~12교시다.

### 요청 3 · 생성 후 이전 선택 확인

- 요청: 지도안 완성 화면에서도 이전 수업 정보·성취기준·수업 모형 선택을 확인한다.
- 상태: 검증 완료
- 반영: 완성 화면 상단에 생성 입력 요약을 추가하고, 도달한 모든 단계로 돌아갈 수 있는 단계 탐색 버튼을 제공한다.
- 파일: `components/lesson-plan/GenerationSummary.jsx`, `components/lesson-plan/StepNavigation.jsx`, `components/lesson-plan/LessonPlanWorkspace.jsx`
- 검증: 요약 표시와 단계 이동 컴포넌트 테스트 통과.
- 제약: 없음.

### 요청 4 · 수정 후 다시 생성

- 요청: 이전 단계 내용을 수정한 뒤 새 지도안을 다시 생성할 수 있게 한다.
- 상태: 검증 완료
- 반영: 마지막으로 생성에 사용한 입력 스냅샷과 현재 입력을 비교해 변경 여부를 알리고, `수정 내용으로 다시 생성`할 수 있게 했다. 새 생성이 실패하면 기존 편집 지도안과 생성 원본을 보존한다.
- 파일: `lib/lesson-input.js`, `lib/draft-store.js`, `components/lesson-plan/LessonPlanWorkspace.jsx`
- 검증: 입력 비교, 이전 단계 이동, 재생성 실패 시 보존 테스트 통과.
- 제약: 없음.

## 21:39 KST · 생성·AI

### 요청 6 · 수업 모형 반영 강화

- 요청: 탐구학습 등 선택한 수업 모형의 실제 단계가 지도안에 반영되도록 점검하고, 필요하면 모형 설명 문서를 생성 참고 자료로 사용한다.
- 상태: 검증 완료
- 반영: 12개 수업 모형별 단계·교사 행동·학생 증거·피해야 할 패턴을 구조화하고 Markdown 참고 문서를 만들었다. 생성 프롬프트가 선택 모형의 단계명을 요구하며, 누락되면 한 차례 자동 보정한다.
- 파일: `lib/instruction-model-guides.js`, `lib/instruction-model-alignment.js`, `lib/upstage/prompts.js`, `docs/instruction-models/*.md`
- 검증: 12개 가이드 완전성, 단계 순서, 생성 경로 보정 테스트 통과. `solar-pro3` 실제 호출에서 직접 교수·탐구·협동·프로젝트·토의·토론 대표 5개 모형이 모두 스키마와 단계 정렬 검사를 통과했다.
- 제약: 생성 문장의 교육적 적절성은 교사가 최종 확인해야 한다.

## 21:39 KST · 교육과정·데이터

### 요청 7 · 교육과정 폴더명 반영

- 요청: 마지막 `복사본` 글자를 지운 교육과정 폴더명을 확인하고 사용한다.
- 상태: 검증 완료
- 반영: 소스 경로를 `2022_Revised_National_Curriculum`로 통일하고 14개 교육과정 문서에서 2,733개 성취기준 카탈로그를 다시 생성했다.
- 파일: `2022_Revised_National_Curriculum/`, `scripts/build-curriculum.mjs`, `data/curriculum-standards.json`
- 검증: 새 경로 계약 및 카탈로그 생성 테스트 통과.
- 제약: 없음.

### 요청 8 · 직접 입력 과목과 공식 과목 연결

- 요청: 과목 목록 선택뿐 아니라 직접 과목을 입력하고, AI가 학교급에 맞는 공식 과목·성취기준을 찾도록 한다.
- 상태: 검증 완료
- 반영: `목록에서 선택`과 `직접 입력` 모드를 제공한다. 직접 입력 과목은 Upstage가 같은 학교급의 공식 과목 후보에 연결하며, 교사가 후보를 확인한 뒤 해당 과목들의 성취기준만 추천·검색한다.
- 파일: `components/lesson-plan/LessonBasicsStep.jsx`, `app/api/map-subject/route.js`, `lib/subject-mapping.js`, `lib/curriculum/search.js`
- 검증: 학교급 제한, 다중 공식 과목 검색, API 오류 대체 후보 테스트 통과.
- 제약: AI 연결 결과는 교사가 최종 확인해야 한다.

## 21:39 KST · 문서 내보내기

### 요청 5 · HWPX 표 찌그러짐과 내용 잘림

- 요청: PDF와 달리 HWPX에서 도입·전개·정리 박스가 뭉개지고 표 내용 일부만 보이는 문제를 수정한다.
- 상태: 구조 검증 완료(한컴오피스 육안 검증 제외)
- 반영: 모든 행을 20pt로 고정하던 방식을 제거하고, 문단 수와 예상 줄바꿈을 기준으로 행 높이를 계산한다. 같은 행의 셀 높이를 통일하고 표 전체 높이를 행 높이 합계로 설정한다.
- 파일: `lib/export/hwpx-style.js`, `lib/export/hwpx.js`, `tests/export-hwpx.test.mjs`
- 검증: 실패 우선 회귀 테스트 후 HWPX 관련 24개 테스트 통과. 생성 XML에서 다양한 행 높이와 정확한 표 높이 합을 확인했다.
- 제약: 로컬 환경에 한컴오피스 렌더러가 없어 앱 자체의 최종 화면 비교는 할 수 없다.

## 21:39 KST · 버그 수정

- 요청: 생성 완료 뒤 편집·이동·재생성이 막히거나 실패 시 기존 결과가 사라지는 문제를 함께 막는다.
- 상태: 검증 완료
- 반영: 현재 단계와 최대 도달 단계를 분리하고, 진행 중 요청을 이전 단계 이동 시 취소하며, 성공 응답이 온 경우에만 기존 지도안을 교체한다.
- 파일: `components/lesson-plan/LessonPlanWorkspace.jsx`, `lib/draft-store.js`
- 검증: 탐색·요청 실패·기존 결과 보존 회귀 테스트 통과.
- 제약: 없음.

## 21:39 KST · 테스트·검증

- 요청: 모형별 반영, 교육과정 경로, 직접 입력 과목, 재생성, HWPX를 각각 확인하고 수정 내역을 계속 추적한다.
- 상태: 검증 완료
- 반영: 단위·컴포넌트·API·문서 내보내기 회귀 테스트를 추가했으며, 이 문서와 `README.md`에 이후 수정 기록 규칙을 정했다.
- 파일: `tests/*.test.mjs`, `e2e/lesson-plan.spec.mjs`, `docs/change-log/`
- 검증: Vitest 36개 파일·277개 테스트, Playwright 데스크톱·모바일 16개 시나리오, Next.js 프로덕션 빌드, 문서 fixture 3종과 HWPX 집중 검사 25개가 모두 통과했다. 375×812·768×1024·1280×900 캡처에서 가로 넘침, `T` 노출, 날짜 누락, 재생성 상태를 확인했고 발견된 데스크톱 날짜 셀 문제를 수정 후 재검증했다.
- 제약: React Doctor는 66/100으로 기존 코드까지 포함한 경고 20개를 보고했다. 이번 변경에서 확인된 버튼 타입 누락은 수정했으며, 파생 상태·기존 배열 키·미사용 파일 등 범위 밖 항목은 별도 정리 대상으로 남긴다. 로컬 환경에 한컴오피스가 없어 HWPX의 앱 자체 렌더링은 사용자가 최종 확인해야 한다.

## 21:57 KST · 테스트·검증

- 요청: 최종 구현이 실제 교사 흐름과 표준 지도안 출력 계약을 함께 만족하는지 완료 검증한다.
- 상태: 검증 완료
- 반영: 직접 과목 연결, 이전 단계 확인, 입력 변경 경고, 성공·실패 재생성, 1·2차시 인쇄, HWPX·DOCX·PDF 다운로드를 한 흐름으로 확인했다.
- 파일: `e2e/lesson-plan.spec.mjs`, `e2e/lesson-plan-export.mjs`, `scripts/check-instruction-model-samples.mjs`
- 검증: 단위·통합 277/277, E2E 16/16, HWPX 집중 25/25, 프로덕션 빌드 성공, Upstage 대표 모형 5/5 PASS.
- 제약: 한컴오피스에서 HWPX를 직접 여는 수동 확인과 독립 시각 검토자 2중 판정은 이 환경에서 수행하지 못했다. 구조 XML, 실제 브라우저 다운로드, 반응형 캡처로 대체 검증했다.

## 23:40 KST · 교육과정·데이터

- 요청: 중학교에서 존재하지 않는 실과를 제거하고 기술·가정과 정보를 분리한다. `중학교 선택` 같은 묶음 이름 대신 한문, 환경, 보건, 진로와 직업을 각각 표시하며 초등학교와 일반고등학교 과목도 교육과정에 맞게 정리한다.
- 상태: 검증 완료
- 반영: 초등은 학년별 교과, 중학교는 공통·선택과목, 고등학교는 공통·주요 선택과목을 구체적인 과목명으로 제공한다. 중학교의 실과를 제거하고 기술·가정과 정보를 분리했으며 한문·환경·보건·진로와 직업을 각각 선택할 수 있다.
- 파일: `lib/subject-options.js`, `components/lesson-plan/LessonBasicsStep.jsx`
- 검증: 학교급·학년별 과목 목록과 `직접 입력`의 마지막 항목 배치를 단위·컴포넌트 테스트로 확인했다.
- 제약: 고등학교의 모든 전문·융합 선택과목을 목록에 싣지 않고 직접 입력으로 보완한다.

## 23:40 KST · 화면·사용성

- 요청: 별도 `과목 입력 방식` 라디오를 없애고 과목 목록 마지막 `직접 입력`을 선택할 때만 입력창을 표시한다.
- 상태: 검증 완료
- 반영: 별도 입력 방식 라디오를 없앴다. 과목 목록 마지막의 `직접 입력`을 선택한 경우에만 입력창과 공식 과목 연결 패널이 표시된다.
- 파일: `lib/subject-options.js`, `components/lesson-plan/LessonBasicsStep.jsx`
- 검증: 컴포넌트 테스트와 375·768·1280px 반응형 캡처로 확인했다.
- 제약: 없음.

## 23:40 KST · 생성·AI

- 요청: 지도안, 학습지, 수행평가·루브릭, 학생 PDF OCR·채점, 세특을 상단 5개 탭으로 연결하고 여러 학생 PDF를 일괄 처리한다.
- 상태: 검증 완료
- 반영: 지도안, 학습지, 수행평가·루브릭, OCR·채점, 세특을 상단 5개 탭으로 연결했다. 단계별 생성 입력 해시, 이전 결과 변경 경고, 교사 승인, 학생별 부분 실패·재시도와 저장 초기화를 적용했다.
- 파일: `components/workflow/`, `lib/workflow-store.js`, `lib/source-hash.js`, `app/page.js`
- 검증: 지도안에서 2명의 세특까지 이어지는 데스크톱·모바일 E2E와 15개 반응형 화면 캡처를 통과했다.
- 제약: 실제 필기체 표본이 없어 생성된 한국어 PDF로 OCR 연결을 검증한다.
