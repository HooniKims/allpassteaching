# 2026-07-13 수정 요청·개선 기록

## 00:54 KST · 보안 · 수행평가 PDF 집계 렌더 방어

- 요청: 개별 필드와 500KB 요청 제한을 통과해도 115쪽 PDF로 커지던 집계 렌더 위험을 최종 검증 전에 차단한다.
- 반영: 수행평가 전체 문자열에 60,000자 집계 예산을 적용하고 PDF 렌더러에 30쪽 최후 방어선을 추가했다. 500,000 UTF-8 바이트 초과 요청은 413, 구조는 맞지만 집계 내용이나 페이지가 과도한 평가는 422로 구분한다.
- 보존: 15개 평가영역·6수준의 최대 구조도 간결한 정상 문구라면 PDF로 저장된다. 표지 1쪽 계약과 교사 편집 데이터는 유지된다.
- 검증: 실패 우선 자원 회귀 테스트에서 차단 전 렌더 완료를 확인한 뒤, 차단 후 집중 테스트와 전체 689/689를 통과했다. 깊이 3,000 JSON, 원소 150,000개 배열, 100,000개 오류 증폭 배열, 렌더하지 않는 60,000자 초과 정합성 메모도 안전한 제한 응답 또는 정상 PDF로 처리한다.

## 00:54 KST · 통합 · 두 학생 5단계 및 산출물 검사

- 요청: 지도안부터 세특까지 두 학생 경로를 실제 다운로드와 부분 실패 복구까지 연결한다.
- 반영: HWPX·DOCX·지도안 PDF, 학생용·교사용 학습지 PDF, 수행평가 표지·전체 PDF, Excel 명단 양식을 브라우저에서 실제 내려받아 검사한다. 합본의 학생별 표지 제외 OCR, 두 번째 학생 OCR 실패·재시도, 수식 무점수 교사 확인, 원본 근거 이동, 승인, 세특 전체 재생성의 한 학생 실패·재시도, 후보 한 명 적용·한 명 유지를 같은 결정적 경로로 검증한다.
- 검사: PDF 시그니처·쪽수, Excel 헤더·행 순서, HWPX/DOCX ZIP·본문 표, 설치된 HWPX 구조 검증기를 확인했다. 한컴 공식 검증기는 현재 환경에서 찾지 못했다.

## 00:54 KST · AI·OCR · 비식별 Upstage 실연결

- Standard: 학생 데이터가 없는 합성 PDF 1건을 실제 호출해 3,365ms, 1쪽, 142자, 요소 4개로 계약을 통과했다.
- Enhanced: 환경에 모델이 설정되지 않아 호출하지 않았고 성능을 추정하거나 성공으로 기록하지 않았다.
- 안전: 합성 수식의 핵심 기호가 이번 호출에서는 일치했지만 `teacher_review`를 유지했다. API 키·원문·원본 응답·학생 이름은 로그에 남기지 않았다.
- 한계: 실제 비식별 한국 학생 손글씨·수학 풀이·과학 도표·학생 그림 표본이 제공되지 않아 정확도 검증은 외부적으로 불가능하다. 소프트웨어는 불확실한 근거를 무점수 교사 확인으로 강제하며 정확도 검증 완료를 주장하지 않는다.

## 00:54 KST · 화면·접근성 · 3개 폭 29상태 재검수

- 범위: 375×812, 768×1024, 1280×900에서 원본 PDF, 근거 연결·누락, 낮은 신뢰도, 교사 수준 선택, 승인, 새로고침 재첨부, 학생 명단, 삭제 확인, Excel 원자적 오류까지 29개 상태를 새로 캡처했다.
- 결과: 콘솔 오류 0건, 문서 가로 넘침 0건, 잘린 조작부 0건, Axe 위반 0건이다. 대표 모바일·태블릿·데스크톱 화면의 Paperlogy 한글 글리프, 줄바꿈, 표와 버튼 잘림을 육안으로 다시 확인했다.

## 00:54 KST · 전체 검증

- Vitest: 79개 파일, 689/689 통과
- Next.js 프로덕션 빌드: 통과, 교육과정 2,733개 재생성, 앱/API 14개 라우트 컴파일
- Playwright: 데스크톱·모바일 24/24 통과
- 프로덕션 서버 수동 게이트: `next start`에서 두 학생 전체 경로 데스크톱·모바일 2/2 통과
- 실제 API: Upstage Standard 1/1 통과, Enhanced 0건(미설정)
- 시각 QA: 29/29 통과
- 문서: HWPX 구조 검증 1/1 통과, PDF·DOCX·Excel 파싱 통과

## 01:12 KST · 보안·최종 독립 검토

- 적대 입력: 깊이 3,000 객체, 원소 150,000개 배열, Zod 오류 100,000개 증폭 입력을 추가 점검했다. 스키마 전 반복 구조 상한은 배열·객체당 100개와 전체 노드 10,000개를 적용하고, 오류 세부정보는 최대 20건만 반환한다.
- 렌더 예산: PDF에 실제 그리는 과제·루브릭·보이는 표지 데이터만 합산한다. 내부 정합성 메모처럼 PDF에 표시하지 않는 데이터는 정상 문서를 차단하지 않는다.
- OCR 계약: Standard와 설정된 Enhanced는 실제 mode, `visualAnalysisStatus`, `requiresVisualReview`, `teacher_review`를 모두 만족해야 스모크를 통과한다. Enhanced의 Standard 폴백은 실패로 처리한다.
- 독립 검토: 요구사항, QA, 코드 품질, 보안·개인정보, 맥락·추적성 5개 검토가 모두 PASS했다. 잔여 CRITICAL/HIGH 항목은 0건이다.
- 최종 수치: Vitest 79파일·689/689, 빌드 PASS, CI E2E 24/24, `next start` 전체 경로 2/2, 시각 29/29, Upstage Standard 1/1, HWPX 구조 검증 1/1.

## 23:39 KST · 문서 내보내기 · HWPX 레이아웃 캐시 제거

- 요청: HWPX 다운로드 시 표·텍스트 레이아웃이 깨지는 문제를 `jkf87/hwpx-skill` 기준으로 실제 점검하고 보완한다.
- 상태: 구조·캐시 검증 완료, 한컴오피스 육안 검증 대기.
- 반영: 공통 HWPX 템플릿의 오래된 `hp:linesegarray`를 제거했다. 이후 생성하는 지도안·학습지·루브릭·수행평가지가 한컴에서 현재 문서 내용으로 줄 배치를 다시 계산한다.
- 파일: `lib/export/hwpx-template/Contents/section0.xml`, `tests/export-hwpx-quality.test.mjs`, `tests/workflow-hwpx.test.mjs`, `tests/assessment-rubric-export.test.mjs`
- 검증: `jkf87/hwpx-skill` 최신 점검 도구(e5e35cb)로 기존 HWPX 6종을 실제 보정·구조 검증했다. 각 파일에서 캐시 1개를 제거한 뒤 모두 `VALID`였고, 새 생성 경로 3종의 캐시 부재 회귀 테스트 16건도 통과했다. 전체 `npm test`는 파일 병렬 실행을 끈 안정 모드에서 90개 파일·765개 테스트를 통과했다.
- 제약: 이 환경에는 한컴오피스 렌더러가 없어 실제 한컴 화면의 쪽 나눔·글꼴 대체를 자동 확인할 수 없다. 다음 HWPX 다운로드는 한컴에서 열어 최종 화면을 한 번 확인한다.
