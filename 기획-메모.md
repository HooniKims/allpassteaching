# 교사용 올인원 교수·학습·평가·기록 웹앱 기획 메모

> 대화에서 확정된 제품 결정을 하나씩 누적하는 문서입니다.

## 제품 방향

- 교사를 위한 올인원 교수·학습·평가·기록 웹앱을 만든다.
- 첫 번째로 구현할 워크플로우는 **수업 지도안 작성**이다.
- 초기 버전은 로그인과 클라우드 저장을 제외한 **MVP**로 만든다.
- AI 기능은 **Upstage API**를 사용한다.
- API 키는 `.env` 파일의 환경 변수로 관리하며, 사용자가 직접 입력한다.
- 디자인은 Refero 디자인 리서치를 바탕으로 이 제품에 맞게 재구성한다.
- 구현은 LazyCodex의 계획·실행 워크플로우를 사용한다.

## 수업 지도안 작성 워크플로우

### 1. 기본 수업 정보

- MVP의 지원 학교급은 **초등학교, 중학교, 일반고등학교**로 제한한다.
- 특성화고 전문교과는 초기 MVP의 검색·추천 범위에서 제외한다.
- 교사가 학교급, 과목, 학년, 가르칠 개념 및 내용을 입력한다.
- 한 차시 수업과 연속 차시 수업을 모두 지원한다.
- 차시 수를 선택할 수 있게 한다.
- 연속 차시는 단원 전체 흐름과 차시별 지도안을 함께 생성한다.

### 2. 2022 개정 교육과정 및 성취기준

- 현재 프로젝트 폴더에 있는 2022 개정 교육과정 자료를 사용한다.
- 선택한 학교급과 과목에 맞는 성취기준만 탐색·선택할 수 있게 한다.
- 교사는 다음 두 방법을 모두 사용할 수 있다.
  1. 목록에서 성취기준을 직접 검색하고 선택한다.
  2. 입력한 개념과 내용을 AI가 분석하여 관련 성취기준을 추천받는다.
- AI는 성취기준을 자동으로 확정하지 않는다.
- AI는 관련 성취기준 후보 3~5개와 추천 이유 및 적합도를 제시한다.
- 최종 성취기준은 반드시 교사가 확인하고 선택한다.

### 3. 수업 모형

- 검증된 수업 모형을 조사하여 목록으로 제공한다.
- 교사가 수업 모형을 선택한 뒤 지도안을 생성한다.
- 선택한 수업 모형의 단계와 특성이 생성 결과에 반영되어야 한다.

### 4. 생성 결과와 내보내기

- 생성한 지도안을 브라우저에서 확인하고 수정·복사할 수 있게 한다.
- 표준 지도안은 다음 순서로 구성한다.
  1. 수업 개요
  2. 성취기준
  3. 학습 목표
  4. 준비물
  5. 수업 모형
  6. 도입·전개·정리(교수 활동, 학생 활동, 시간, 자료·유의점)
  7. 과정중심평가
  8. 개별화·지원 전략
  9. 수업 후 성찰
- 내보내기 형식을 다운로드 시 선택할 수 있게 한다.
- 지원 형식은 **HWPX, DOCX, PDF**이다.

## 핵심 원칙

- AI는 교사의 결정을 대신하지 않고, 근거 있는 후보를 제안한다.
- 교육과정과 성취기준의 최종 선택권은 교사에게 둔다.
- 학교급·과목에 맞지 않는 성취기준이 노출되거나 선택되지 않게 한다.
- 확정되지 않은 기능은 이 문서의 확정 항목에 기록하지 않는다.

## 기술·데이터 구조 결정

- 교육과정 검색과 AI 추천은 **하이브리드 구조**로 구현한다.
- 교육과정 Markdown에서 성취기준 코드와 설명을 추출하여 학교급·학년군·과목별 정적 JSON으로 구조화한다.
- 학교급·과목·학년군 필터는 로컬 데이터 계층에서 먼저 강제한다.
- 교사가 입력한 개념과 내용으로 로컬 검색 후보를 좁힌 후 Upstage API가 의미 적합도, 추천 이유, 적합도 점수를 분석한다.
- 긴 교육과정 원문 전체를 요청마다 Upstage API로 전송하지 않는다.
- 최종 성취기준 선택은 교사가 수행한다.

## 디자인 방향

- Refero 리서치 기반 시안 중 **A안: 차분한 교사 워크벤치**를 사용한다.
- 주된 시각 기반은 문서 작업에 집중하기 좋은 Notion 계열의 작업공간 구조로 한다.
- Super 스타일에서 단일 행동색을 제한적으로 사용하는 원칙을 차용한다.
- 따뜻한 중립색 캔버스, 흰색 작업 면, 얇은 경계선, 짙은 녹색 기본 행동색을 사용한다.
- 전체 UI 글꼴은 **Paperlogy**를 사용한다.
- 공식 배포본 v1.001을 프로젝트에 내려받아 외부 CDN 없이 로컬 호스팅한다.
- 본문은 Regular(400), UI 레이블과 입력값은 Medium(500), 버튼과 소제목은 SemiBold(600), 주요 제목은 Bold(700)를 기본으로 사용한다.
- `font-display: swap`과 시스템 산세리프 대체 글꼴을 설정한다.
- Paperlogy의 SIL Open Font License(OFL) 전문을 폰트 자산과 함께 보존한다.
- 불필요한 카드, 그라데이션, 장식용 애니메이션, 보라색 계열 AI 도구 디자인을 사용하지 않는다.
- 왼쪽에는 전체 작성 단계를 지속적으로 보여주고, 오른쪽에는 현재 단계의 작업에 집중하는 구조를 사용한다.

## 화면 흐름

- 수업 지도안 작성은 다음 4단계로 진행한다.
  1. 수업 정보: 학교급, 학년, 과목, 차시 수, 수업 개념 및 내용 입력
  2. 성취기준: 직접 검색·선택 또는 AI 추천 후보 검토 후 선택
  3. 수업 모형: 수업 목적에 맞는 추천과 설명을 확인한 후 선택
  4. 지도안 완성: 생성, 브라우저 편집, 형식 선택 및 내보내기
- 데스크톱에서는 왼쪽에 단계 탐색을 고정하고 오른쪽 작업 영역만 전환한다.
- 모바일에서는 단계 탐색을 상단 진행 표시로 축약한다.
- 입력 내용과 생성 결과는 브라우저에 임시 저장하여 새로고침 후 복구한다.
